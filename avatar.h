// Guardas:
#ifndef _AVATAR_H_
#define _AVATAR_H_

// Librerías a usar:
#include <iostream>
#include <sstream>
#include <vector>
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string>
#include <fstream>
#include <math.h>

using std::string;
using namespace std;

// Clase:
class Avatar {

public:
// Constructor de la clase:
	Avatar();
// Destructor de la clase:
  ~Avatar();
// Métodos:

protected:

private:
// Atributos y/o variables:
  
};

#endif 